name: gold-distill
version: 1.0.0
# GOLD knowledge distillation (generalized JSD) of a small student from a larger
# teacher, using the kd-sandbox trainer on BlueVela via the SkyPilot LSF cloud.
#
# Division of labour, which is what keeps this step small: SkyPilot's LSF
# provisioner already owns the bsub submission, the blaunch fan-out, enroot image
# import/creation, and the per-node topology (RANK / TOTAL_NODES / MASTER_ADDR).
# So the launcher scripts from the ansible path are NOT ported here — this step
# replaces only their inner logic: render the trainer's config, then start
# accelerate with this node's rank.
#
# Deps come from the image; the trainer itself comes from the live /proj checkout
# (kd_code_dir), which the provisioner identity-mounts into the container. A
# trainer fix therefore lands without touching granite.build, at the cost of the
# checkout being unpinned state — so the step records its commit as step metadata.
type: training
config:
  # ─── Source delivery ──────────────────────────────────────────────────────────
  # IDENTICAL IN EVERY PORTED DISTILLATION STEP. The block and the delimited region
  # of `run` below are asserted byte-identical across the ported steps by
  # test/test_source_contract.py — one contract, learned once. Change it here and
  # the other steps' tests fail until they are changed to match.
  code_config:
    # THE DEFAULT: a checkout on the shared filesystem, exactly as gold-distill gets
    # the kd-sandbox trainer from `kd_code_dir`. One pattern in the branch, not two.
    #
    # NO CREDENTIAL REACHES THE CONTAINER on this path, and that is the point. Every
    # other step in this repo either clones nothing or clones a public repo
    # unauthenticated (byoc: "Public repos only"); granite.build's own private-repo
    # access clones SERVER-SIDE on the gbserver host. Shipping a git token into a
    # compute node would be new ground, and the blast radius is not worth it — a
    # token that can read this repo is necessarily broad, because the repo is owned
    # by a personal namespace and a fine-grained PAT cannot scope to it at all.
    #
    # Refresh it in place when the pin below moves; the cluster account has its own
    # GHE key, so cluster-side `git fetch && git checkout <sha>` needs no secret:
    #   git -C /proj/granite-build/g4os/gb-steps-collection-post-training fetch --all
    #   git -C /proj/granite-build/g4os/gb-steps-collection-post-training checkout <sha>
    code_dir: "/proj/granite-build/g4os/gb-steps-collection-post-training"
    # The commit code_dir is EXPECTED to be at. Recorded either way, but when this is
    # set and the checkout is at a different commit the step says so loudly rather
    # than training against whatever happens to be checked out. Empty => no check.
    expect_ref: "70c1550a171aa8e09a9ad9047a5bf763c39e8579"
    # OVERRIDE, for a host with no /proj: clone at run time instead. Used ONLY when
    # code_dir does not resolve. https, because the token path below is https-only.
    repo: ""
    # Branch, tag or commit checked out after that clone. A branch head makes two
    # runs a week apart different runs while reporting the same provenance.
    ref: ""
    # Clone destination, RELATIVE to the step's working directory. On BlueVela the
    # launcher cd's into $GB_BUILD_WORKDIR, which is on /proj — identity-mounted
    # into the container at the same path, so a relative dir is container-visible.
    workdir: "distill-code"
    # The NAME of a space secret holding a read-scoped credential — never a value.
    # Only consulted on the clone path. gbserver merges the space's secrets into the
    # task environment by name (gbserver/environment/skypilot.py:
    # `env.update(self.secrets)`), so the run block reads it as $<this name>;
    # verified on BlueVela. Left EMPTY on purpose: the default path needs no
    # credential. gitstore's own convention for the ssh variant is `GIT_SSH_KEY`
    # (gbserver/asset/gitstore.py), and a read-only deploy key there would be the
    # tightest option if this ever has to be automated.
    token_secret: ""
    # The interpreter. NOT a bare `python`: this image's venv is not on PATH, and a
    # bare python resolves to the system interpreter, which has no transformers.
    python: "/stage/.venv/bin/python"
    # Optional bash run after the source resolves and before the workload, from the
    # step's working directory. Empty => skipped. Not needed on this image: it
    # already carries torch 2.8.0, transformers 5.8.0, tokenizers 0.22.1 and
    # safetensors 0.7.0 (measured, BlueVela job on p6-r17-n3).
    setup_command: ""
  gold_config:
    # ── Code and image ──────────────────────────────────────────────────────
    # Live kd-sandbox checkout on the shared filesystem. Supplies gold/*.py and
    # configs/deepspeed/. Set to the in-image snapshot (/stage) to pin instead.
    kd_code_dir: "/proj/granite-build/g4os/kd-sandbox"
    # Deliver gb_steps_post_training to the container as well, via the same
    # shared-checkout block every other ported distillation step uses. OFF by
    # default, and off is the pre-existing behaviour: this step's trainer comes
    # from kd_code_dir above, and nothing in the default path imports that
    # package. Turn it on to reach the extra validators the renderer can then
    # apply (--corpus-path, --resume, --nodes/--gpus-per-node); those flags
    # FAIL LOUDLY rather than skipping if this is off, because a validator that
    # silently does not run is worse than one that is absent.
    deliver_distill_source: false
    # DeepSpeed ZeRO-3 accelerate config, relative to kd_code_dir.
    ds_config: "configs/deepspeed/accelerate_deepspeed_zero3.yaml"
    # Names the checkpoint directory and the rendered config. The node count is
    # appended at runtime, from the allocation, so the two cannot disagree.
    run_name: "gold"

    # ── Model and data ──────────────────────────────────────────────────────
    model_name_or_path: ""          # student init
    teacher_model_name_or_path: ""  # frozen, forward-only in the training loop
    # Must be think-filtered ("nothink"): an assistant completion containing an
    # inline <think>...</think> breaks gold's completion extraction.
    dataset_name: ""

    # ── Training hyperparameters ────────────────────────────────────────────
    num_train_epochs: 1.0
    # Cap the run by optimizer steps rather than by epochs. 0 leaves the key out
    # of the rendered config entirely, so an epoch-bounded run is unaffected;
    # any positive value overrides num_train_epochs in the trainer. This is what
    # a run over a corpus too large to epoch through needs — one epoch of the
    # 802,027-row reference corpus is 4,177 steps at effective batch 192.
    max_steps: 0
    learning_rate: 1.0e-05
    min_lr: 1.0e-06
    warmup_ratio: 0.05
    lr_scheduler_type: cosine_with_min_lr
    per_device_train_batch_size: 1
    # Effective batch = per_device * grad_accum * nodes * gpus_per_node. The
    # reference run is 192 (1 x 6 x 4 x 8); halving the nodes wants 12 here to
    # hold that, since the LR schedule was tuned against it.
    gradient_accumulation_steps: 6
    max_completion_length: 4096
    max_length: 16384
    gradient_checkpointing: true
    # Keep the whole trajectory: an early, less-forgotten checkpoint is often the
    # best one to evaluate, and a small limit deletes it irrecoverably.
    # The renderer has always emitted this; it is a config key so a run can turn
    # checkpointing off ("no") without editing the step. "steps" pairs with
    # save_steps below and is what every validated reference config sets.
    save_strategy: "steps"
    save_steps: 500
    save_total_limit: 20
    logging_steps: 5
    dataset_num_proc: 64

    # ── GOLD / distillation loss ────────────────────────────────────────────
    temperature: 1.0
    # 0 => off-policy: every node trains on the fixed dataset, no vLLM.
    lmbda: 0.0
    beta: 0.0
    # granite's logits_scaling=10 overflows the fused bf16 JSD kernel -> NaN loss.
    use_liger_fused_jsd: false
    # The objective as a validated PRESET, instead of nine booleans a build may combine
    # freely. EMPTY BY DEFAULT and empty means unchanged: the flags above stay
    # authoritative, every existing recipe renders exactly the config it rendered before,
    # and the escape hatch those recipes' READMEs document (--param
    # USE_LIGER_FUSED_JSD=true) keeps working.
    #
    # Naming an arm buys two things a boolean cannot. First, the arm sets the switches the
    # renderer has no flag for at all (use_kl_interpolation, use_adaptive_kld, use_distillm2,
    # use_distillm2_like, use_reversed_distillm2_like, use_uld_loss, use_ce_loss). Second,
    # it enforces that arm's requirements -- several of which exist because OUTSIDE them the
    # arm does not fail, it silently becomes a different arm, which no later step could
    # detect. See LOSS_ARMS in src/render_gold_config.py, which lists each arm and why.
    loss_arm: ""
    # Verify the student's tokenizer is the one the corpus was built with. Off by default
    # because it needs deliver_distill_source: true above; ON is worth it for any build that
    # chains distill-corpus-prep into this step, because a mismatch does not error -- it
    # trains on mis-segmented text at full speed with a falling loss.
    check_corpus_tokenizer: false
    # Required for completion masking; the nothink chat template has no
    # {% generation %} tag, so this is what locates the span.
    #
    # A trailing line boundary must be written as the two-character escape \n, not
    # as a real newline: gbserver fills every config string through Jinja, which
    # strips exactly one trailing newline from each value, so a real one never
    # reaches this step. render_gold_config.py decodes the escape once, in the
    # container. The default below wants no line boundary and so carries none.
    response_template: "<|im_start|>assistant"

    # ── NCCL / distributed diagnostics ──────────────────────────────────────
    # Empty disables NCCL's own logging. Set to INFO (with nccl_debug_subsys
    # INIT,NET) to see which transport and HCA each rank selected — the only way
    # to tell an IB path from a silent TCP fallback, or to see where a collective
    # stalls. Verbose, so off by default.
    nccl_debug: ""
    nccl_debug_subsys: "INIT,NET"
    # Collective timeout. The reference launcher uses 3600000 (1 h) to survive a
    # slow 30B teacher forward without spurious aborts. Lower it for debugging so
    # a hang reports in minutes rather than tying up an allocation.
    nccl_timeout_ms: 3600000
    # Whether torch's monitor thread may abort a stalled collective. The
    # reference launcher disables this; we default it ON deliberately, because
    # with it off a deadlock does not fail — it hangs forever holding the
    # allocation, producing no diagnostic at all. Set false only to reproduce the
    # reference launcher's exact behaviour.
    nccl_enable_monitoring: true

    # ── On-policy (online), default off ─────────────────────────────────────
    # >0 dedicates the LAST N nodes to serving the student under vLLM and trains
    # on the rest. On-policy continues from a good off-policy checkpoint, so set
    # model_name_or_path to that checkpoint rather than the base overlay.
    vllm_num_servers: 0
    # An EXTERNAL vLLM server, reached by URL, instead of carving nodes out of
    # this allocation. Set from another target's mem:// binding, e.g.
    #   vllm_server_url: "{{ '{{' }} bindings.vllm.binding.state {{ '}}' }}"
    # When set, EVERY node here trains and the role split below does not happen;
    # the trainer is given the address on gold.py's command line. Empty keeps the
    # in-allocation split that vllm_num_servers alone selects.
    #
    # NOT YET RUN ON A CLUSTER. The trainer pushes updated student weights to the
    # server over NCCL (the reference launcher's VLLM_NCCL_COORDINATOR_PORT), so
    # an external server means a NCCL process group spanning two LSF allocations.
    # Whether that works here is the open question this path exists to answer.
    vllm_server_url: ""
    # server = talk to a vLLM HTTP server; the reference launcher's value. Only
    # consulted on the on-policy path.
    vllm_mode: "server"
    # How often, in optimizer steps, the trainer pushes its updated weights to the
    # server. The reference launcher uses 1: on-policy means generating from the
    # CURRENT student, so a larger value samples from a staler policy.
    vllm_sync_frequency: 1
    top_p: 0.95
    use_sampled_opd_loss: false
    last_message_only: false
    clip_alpha: 0.1
    opd_importance_sampling: false

outputs:
  optional:
    checkpoint:
      type: model

environment_configs:
  Skypilot:
    # enroot + the LSF topology contract this step reads are LSF-specific, and
    # the image is an SM90 (H100) build that cannot run on A100.
    subtypes:
      - lsf
    default_launcher: gold
    launchers:
      gold:
        type: skypilot
        monitors:
          - skypilot_monitor
        config:
          image_id: "docker:us.icr.io/cil15-shared-registry/kd-sandbox-distill:0.1.0-uv"
          # Node count and accelerators come from the build.yaml
          # (compute_config.num_nodes and launcher_config.resources) so one step
          # serves the smoke and reference runs.
          resources: {}
          # Ships the config renderer. A relative dest lands under
          # $GB_BUILD_WORKDIR, which on BlueVela is on /proj — identity-mounted
          # into the container at the same path and exempt from symlink-wrapping.
          file_mounts:
            src: src
          envs:
            HF_HOME: /opt/hf-cache
            TORCH_NCCL_ENABLE_MONITORING: '{{ "1" if config.gold_config.nccl_enable_monitoring else "0" }}'
            TORCH_NCCL_TIMEOUT_MS: "{{ config.gold_config.nccl_timeout_ms }}"
            NCCL_TIMEOUT: "{{ config.gold_config.nccl_timeout_ms }}"
            NCCL_DEBUG: "{{ config.gold_config.nccl_debug }}"
            NCCL_DEBUG_SUBSYS: "{{ config.gold_config.nccl_debug_subsys }}"
            USE_LIBUV: "0"
            PYTORCH_CUDA_ALLOC_CONF: expandable_segments:True
            DS_SKIP_CUDA_CHECK: "1"
            OMP_NUM_THREADS: "8"
          # NCCL_IB_HCA / NCCL_SOCKET_IFNAME are deliberately absent: the
          # environment's nccl_tuning_file is sourced outside the container
          # before the enroot config is written, and the generated environ()
          # forwards the outer environment. Confirm on a real run with
          # `grep NCCL_ job.log`; if that file does not set them, add both here.
          run: |
            # The image's venv is not on PATH by default. The dispatcher runs this
            # with `bash <file>` (non-login), so this export sticks — a login
            # shell would re-run /etc/profile and drop it.
            export PATH=/stage/.venv/bin:$PATH

            {% if config.gold_config.deliver_distill_source %}
            # --- distill source delivery: BEGIN (identical in all ported steps) ---
            # `set -eu` is already in effect: the launcher prefixes it.
            CODE_DIR="{{ config.code_config.code_dir }}"
            REPO="{{ config.code_config.repo }}"
            REF="{{ config.code_config.ref }}"
            PYBIN="{{ config.code_config.python }}"
            WORK="${GB_BUILD_WORKDIR:-$PWD}"

            if [ -n "$CODE_DIR" ] && [ -d "$CODE_DIR/src/gb_steps_post_training" ]; then
              echo "distill: source = filesystem checkout $CODE_DIR"
              CODE_SOURCE="filesystem"
              # A shared checkout is mutable state, so say what is actually there.
              # Uncommitted edits are not fatal — someone may be deliberately testing
              # a fix — but a run whose code came from a dirty tree must not report a
              # bare commit as its provenance.
              DIRTY="$(git -C "$CODE_DIR" status --porcelain 2>/dev/null | wc -l | tr -d ' ')"
              if [ "${DIRTY:-0}" != "0" ]; then
                echo "distill: WARNING: $CODE_DIR has $DIRTY uncommitted change(s)." >&2
                echo "         This run's code is NOT the commit recorded below." >&2
              fi
              echo "GB_STEP_METADATA_KEY:distill_code_dirty GB_STEP_METADATA_VALUE:${DIRTY:-0}"
              # And fail loudly if the checkout is not where the recipe expects. A
              # silently-moved shared checkout is how two runs that report the same
              # pin end up having trained on different code.
              EXPECT_REF="{{ config.code_config.expect_ref }}"
              if [ -n "$EXPECT_REF" ]; then
                AT="$(git -C "$CODE_DIR" rev-parse HEAD 2>/dev/null || true)"
                if [ -n "$AT" ] && [ "$AT" != "$EXPECT_REF" ]; then
                  echo "distill: FATAL: $CODE_DIR is at $AT but code_config.expect_ref" >&2
                  echo "         is $EXPECT_REF. Either check that checkout out to the" >&2
                  echo "         expected commit, or change expect_ref deliberately:" >&2
                  echo "           git -C $CODE_DIR fetch --all" >&2
                  echo "           git -C $CODE_DIR checkout $EXPECT_REF" >&2
                  exit 1
                fi
              fi
            elif [ -n "$REPO" ]; then
              CODE_DIR="$WORK/{{ config.code_config.workdir }}"
              CODE_SOURCE="git"
              TOKEN_VAR="{{ config.code_config.token_secret }}"
              # Indirect expansion, with :- so `set -u` does not abort when the space
              # has no such secret. The dispatcher runs this with `bash <file>`, so
              # bash-isms are available.
              TOKEN="${!TOKEN_VAR:-}"
              echo "distill: source = git clone $REPO (ref=${REF:-<default>}) -> $CODE_DIR"
              rm -rf "$CODE_DIR"
              if [ -n "$TOKEN" ]; then
                # GIT_ASKPASS, not a token in the URL: the URL form puts the secret in
                # argv (readable via /proc on a shared node) and persists it into
                # .git/config on the SHARED filesystem. The helper is written 0700 into
                # the node's own tmp and deleted immediately after use.
                ASKPASS="$(mktemp)"
                cat > "$ASKPASS" <<'ASKPASS_EOF'
            #!/bin/sh
            case "$1" in
              Username*) printf '%s' "x-access-token" ;;
              *)         printf '%s' "$GB_DISTILL_TOKEN" ;;
            esac
            ASKPASS_EOF
                chmod 700 "$ASKPASS"
                CLONE_RC=0
                GB_DISTILL_TOKEN="$TOKEN" GIT_ASKPASS="$ASKPASS" GIT_TERMINAL_PROMPT=0 \
                  git clone --quiet "$REPO" "$CODE_DIR" || CLONE_RC=$?
                rm -f "$ASKPASS"
                if [ "$CLONE_RC" != "0" ]; then
                  echo "distill: FATAL: clone of $REPO failed (rc=$CLONE_RC) using the token in" >&2
                  echo "         \$$TOKEN_VAR. Check that secret's scope, not this step." >&2
                  exit 1
                fi
              else
                echo "distill: FATAL: code_config.repo is set but this space has no secret" >&2
                echo "         named '$TOKEN_VAR'. The upstream repo is PRIVATE, so an" >&2
                echo "         unauthenticated clone cannot succeed. Either create that" >&2
                echo "         secret, or set code_config.code_dir to a checkout and leave" >&2
                echo "         code_config.repo empty." >&2
                exit 1
              fi
              if [ -n "$REF" ]; then
                git -C "$CODE_DIR" checkout --quiet "$REF"
              fi
              if [ ! -d "$CODE_DIR/src/gb_steps_post_training" ]; then
                echo "distill: FATAL: $REPO@${REF:-default} has no src/gb_steps_post_training." >&2
                exit 1
              fi
            else
              echo "distill: FATAL: no source. code_config.code_dir ('$CODE_DIR') does not" >&2
              echo "         contain src/gb_steps_post_training, and code_config.repo is empty." >&2
              exit 1
            fi

            # The package ROOT, not the distillation/ directory: every module is
            # imported by its full dotted path, so a flat path does not resolve. The
            # absence of this export is what killed two upstream jobs at import time.
            export PYTHONPATH="$CODE_DIR/src${PYTHONPATH:+:$PYTHONPATH}"

            # Lineage. A filesystem checkout is mutable and a clone may name a branch,
            # so record the resolved commit either way. NEVER fail the build for
            # metadata — and written as a full if/fi rather than `[ -n "$X" ] && echo`,
            # which under `set -e` aborts the run when X is empty.
            CODE_SHA="$(git -C "$CODE_DIR" rev-parse HEAD 2>/dev/null || true)"
            if [ -n "$CODE_SHA" ]; then
              echo "GB_STEP_METADATA_KEY:distill_code_commit GB_STEP_METADATA_VALUE:$CODE_SHA"
            else
              echo "distill: WARNING: $CODE_DIR is not a git checkout, so this run has no" >&2
              echo "         record of which code ran." >&2
            fi
            echo "GB_STEP_METADATA_KEY:distill_code_source GB_STEP_METADATA_VALUE:$CODE_SOURCE"
            {% if config.code_config.setup_command %}
            echo "distill: running setup_command in $(pwd)"
            {{ config.code_config.setup_command }}
            {% endif %}
            # --- distill source delivery: END ---
            {% endif %}

            # Snapshot the node-level topology the provisioner exported, then
            # unset it. accelerate assigns per-process rank itself and would be
            # confused by inherited RANK/WORLD_SIZE; these values belong on its
            # command line instead.
            NODE_RANK="${RANK:-0}"
            NODES="${TOTAL_NODES:-1}"
            GPUS="${NUM_GPUS_PER_NODE:-8}"
            MADDR="${MASTER_ADDR:?MASTER_ADDR not set by the provisioner}"
            MPORT="${MASTER_PORT:-29500}"
            unset RANK WORLD_SIZE LOCAL_RANK MASTER_ADDR MASTER_PORT

            # accelerate gets an IP, not a hostname. The provisioner exports
            # MASTER_ADDR as a short hostname; the reference launcher resolves it
            # from /etc/hosts and passes the address. Rendezvous works either way,
            # but NCCL's bootstrap picks its interface from this value, so keep it
            # identical to the validated path rather than assuming equivalence.
            MIP="$(grep -w "$MADDR" /etc/hosts 2>/dev/null | awk '{print $1; exit}')"
            if [ -z "$MIP" ]; then
              MIP="$(getent ahostsv4 "$MADDR" 2>/dev/null | awk '{print $1; exit}')"
            fi
            [ -z "$MIP" ] && MIP="$MADDR"
            echo "gold-distill: master $MADDR -> $MIP:$MPORT"

            KD_CODE_DIR="{{ config.gold_config.kd_code_dir }}"
            VLLM_SERVERS={{ config.gold_config.vllm_num_servers | int }}

            # Identity derives from the ALLOCATION, never from a build parameter,
            # so the checkpoint path cannot claim a node count the run did not have.
            CONFIG_NAME="{{ config.gold_config.run_name }}_node${NODES}"
            CKPT_DIR="${GB_BUILD_WORKDIR:-$PWD}/checkpoints/${CONFIG_NAME}"
            CFG="${GB_BUILD_WORKDIR:-$PWD}/gold_config_${CONFIG_NAME}.yaml"

            # The trainer comes from a mutable shared checkout, so record which
            # commit ran. Never fail the build for lineage metadata.
            if [ "$NODE_RANK" = "0" ]; then
              KD_SHA="$(git -C "$KD_CODE_DIR" rev-parse HEAD 2>/dev/null || true)"
              [ -n "$KD_SHA" ] && echo "GB_STEP_METADATA_KEY:kd_sandbox_commit GB_STEP_METADATA_VALUE:$KD_SHA"
            fi

            # Rendered per node (each has its own container filesystem) but into
            # a shared path, so the content must be identical on every node — it
            # is, since every input is a build parameter except the node count.
            /stage/.venv/bin/python ./src/render_gold_config.py \
              --output "$CFG" \
              --total-nodes "$NODES" \
              --model-name-or-path "{{ config.gold_config.model_name_or_path }}" \
              --teacher-model-name-or-path "{{ config.gold_config.teacher_model_name_or_path }}" \
              --dataset-name "{{ config.gold_config.dataset_name }}" \
              --num-train-epochs {{ config.gold_config.num_train_epochs }} \
              --max-steps {{ config.gold_config.max_steps }} \
              --learning-rate {{ config.gold_config.learning_rate }} \
              --min-lr {{ config.gold_config.min_lr }} \
              --warmup-ratio {{ config.gold_config.warmup_ratio }} \
              --lr-scheduler-type "{{ config.gold_config.lr_scheduler_type }}" \
              --per-device-train-batch-size {{ config.gold_config.per_device_train_batch_size }} \
              --gradient-accumulation-steps {{ config.gold_config.gradient_accumulation_steps }} \
              --max-completion-length {{ config.gold_config.max_completion_length }} \
              --max-length {{ config.gold_config.max_length }} \
              --gradient-checkpointing {{ config.gold_config.gradient_checkpointing }} \
              --save-strategy "{{ config.gold_config.save_strategy }}" \
              --save-steps {{ config.gold_config.save_steps }} \
              --save-total-limit {{ config.gold_config.save_total_limit }} \
              --logging-steps {{ config.gold_config.logging_steps }} \
              --dataset-num-proc {{ config.gold_config.dataset_num_proc }} \
              --temperature {{ config.gold_config.temperature }} \
              --lmbda {{ config.gold_config.lmbda }} \
              --beta {{ config.gold_config.beta }} \
              --use-liger-fused-jsd {{ config.gold_config.use_liger_fused_jsd }} \
              --loss-arm "{{ config.gold_config.loss_arm }}" \
              --check-corpus-tokenizer {{ config.gold_config.check_corpus_tokenizer }} \
              --response-template '{{ config.gold_config.response_template }}' \
              --vllm-num-servers {{ config.gold_config.vllm_num_servers }} \
              --top-p {{ config.gold_config.top_p }} \
              --use-sampled-opd-loss {{ config.gold_config.use_sampled_opd_loss }} \
              --last-message-only {{ config.gold_config.last_message_only }} \
              --clip-alpha {{ config.gold_config.clip_alpha }} \
              --opd-importance-sampling {{ config.gold_config.opd_importance_sampling }} \
              --vllm-server-url "{{ config.gold_config.vllm_server_url }}"

            if [ "$NODE_RANK" = "0" ]; then
              echo "--- rendered gold config ($CFG) ---"
              cat "$CFG"
              echo "--- end gold config ---"
            fi
            mkdir -p "$CKPT_DIR"

            # Node roles, three cases.
            #
            # 1. EXTERNAL server (vllm_server_url set): the server lives in another
            #    target's allocation and this one is all trainers. The address is
            #    split out of the URL here rather than templated, because it is only
            #    known at run time — it arrives through a mem:// binding.
            # 2. In-allocation split (vllm_server_url empty, vllm_num_servers > 0):
            #    the LAST N nodes serve, matching the reference launcher.
            # 3. Off-policy (the default): every node trains.
            VLLM_URL="{{ config.gold_config.vllm_server_url }}"
            VLLM_ARGS=""
            if [ -n "$VLLM_URL" ]; then
              # http://host:port -> host, port. Strip the scheme, then split on the
              # LAST colon so an IPv6 literal or a missing port is not silently
              # mangled into something that fails at connect time instead of here.
              _hostport="${VLLM_URL#*://}"
              _hostport="${_hostport%%/*}"
              case "$_hostport" in
                *:*) VLLM_HOST="${_hostport%:*}"; VLLM_PORT="${_hostport##*:}" ;;
                *)   VLLM_HOST="$_hostport";      VLLM_PORT="8001" ;;
              esac
              if [ -z "$VLLM_HOST" ]; then
                echo "gold-distill: FATAL: could not parse a host out of vllm_server_url '$VLLM_URL'" >&2
                exit 1
              fi
              TRAINER_NODES="$NODES"
              VLLM_ARGS="--vllm_mode {{ config.gold_config.vllm_mode }}"
              VLLM_ARGS="$VLLM_ARGS --vllm_sync_frequency {{ config.gold_config.vllm_sync_frequency }}"
              VLLM_ARGS="$VLLM_ARGS --vllm_server_host $VLLM_HOST --vllm_server_port $VLLM_PORT"
              echo "gold-distill: external vLLM server at $VLLM_HOST:$VLLM_PORT; all $NODES node(s) train"
            else
              TRAINER_NODES=$(( NODES - VLLM_SERVERS ))
              if [ "$VLLM_SERVERS" -gt 0 ] && [ "$NODE_RANK" -ge "$TRAINER_NODES" ]; then
                echo "gold-distill: rank $NODE_RANK serving the student under vLLM"
                exec /stage/.venv/bin/python "$KD_CODE_DIR/gold/run_vllm_serve.py" \
                  --port "${KD_VLLM_API_PORT:-8001}" \
                  --model "{{ config.gold_config.model_name_or_path }}" \
                  --gpu-memory-utilization 0.9 \
                  --tensor-parallel-size 1 \
                  --data-parallel-size "$GPUS" \
                  --log_level warning \
                  --max-model-len {{ config.gold_config.max_length }}
              fi
            fi

            # --use_vllm is stated in BOTH directions below, from the same
            # vllm_num_servers that gates every other on-policy key. gold.py
            # defaults it to False and custom_gold_trainer.py:2003 branches on it:
            # False sends generation down the local ZeRO-3 path
            # (generate_on_policy_outputs) rather than to the server. Emitted only
            # negatively — as it was until now — an on-policy run allocates a vLLM
            # node, health-checks it, plumbs its address in, then generates locally
            # and leaves the server idle. Build d77546a9 did exactly that, logged
            # not one request against the server, and died in the local path.
            echo "gold-distill: rank $NODE_RANK/$NODES training ($TRAINER_NODES trainer node(s)), master $MADDR:$MPORT"
            accelerate launch \
              --num_processes $(( TRAINER_NODES * GPUS )) \
              --num_machines "$TRAINER_NODES" \
              --machine_rank "$NODE_RANK" \
              --main_process_ip "$MIP" \
              --main_process_port "$MPORT" \
              --config_file "$KD_CODE_DIR/{{ config.gold_config.ds_config }}" \
              "$KD_CODE_DIR/gold/gold.py" \
              --config "$CFG" \
              --output_dir "$CKPT_DIR" \
              --use_vllm={{ 'True' if config.gold_config.vllm_num_servers | int > 0 else 'False' }} \
              $VLLM_ARGS

            # Rank 0 only: the executor streams every node's output into one
            # driver log, so an unguarded marker would register N artifacts.
            if [ "$NODE_RANK" = "0" ]; then
              echo "GB_ARTIFACT_ID:checkpoint GB_ARTIFACT_PATH:${CKPT_DIR}"
            fi
    monitors:
      skypilot_monitor:
        # Artifact/binding convention from the shipped library; overridden to
        # `periodic` retrieval because a distillation run lasts hours and the
        # default on_completion surfaces nothing until the end.
        ref: space://monitors/skypilot
        config:
          poll_interval_seconds: "{{ config.poll_interval_seconds | default(900) }}"
          log_retrieval:
            mode: "{{ config.log_retrieval_mode | default('periodic') }}"
            interval_seconds: "{{ config.log_retrieval_interval_seconds | default(900) }}"
          extra_event_configs:
          # TRL logs a metrics dict, not open-instruct's "Step: N" banner. Pin
          # this against real job_log output from the first smoke run.
          - event_type: WORKLOAD_STATUS_EVENT
            line_regex: "'loss':"
            event_fields:
              - field_name: status
                field_value_template: "RUNNING"
              - field_name: message
                field_regex: "\\{'loss'.+"
